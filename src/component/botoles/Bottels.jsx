import React, { use } from 'react';

const Bottels = ({allbottels}) => {

    const usedata=use(allbottels);
    console.log(usedata);
    
    return (
        <div>
            
        </div>
    );
};

export default Bottels;